apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: foo-production-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: foo-stc
  volumeMode: Filesystem